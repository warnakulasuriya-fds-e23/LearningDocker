version: "3"
services: #services means the docker containers
  dockernodeapp:
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
